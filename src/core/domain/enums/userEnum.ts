export enum GenderType {
    Male = "male",
    Female = "female",
  }

  export enum RoleType {
    Client = "client",
    Admin = "admin",
  }

  export enum StatusType {
    Active = "actived",
    InActive = "Inactive",
  }

  export enum TypeUse {
    Normal = "normal",
    WithGG = "withgg"
  }